-- Comentario general del grupo/proyecto (la tabla todavia esta vacia,
-- se puede anadir directamente como obligatoria sin valor por defecto).
ALTER TABLE "ProyectoGrupo" ADD COLUMN "comentarios" TEXT NOT NULL;
